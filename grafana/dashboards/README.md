Place exported Grafana dashboard JSON files in this directory. The provisioning provider `dashboard_providers.yaml` points Grafana at `/var/lib/grafana/dashboards` to load any JSON files placed here.
